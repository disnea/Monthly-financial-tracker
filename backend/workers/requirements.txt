# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
pydantic==2.6.1
pydantic-settings==2.1.0

# Celery & Message Queue
celery==5.3.6
redis==5.0.1

# HTTP client (for API calls from tasks)
httpx==0.26.0

# Authentication (if tasks need to validate tokens)
python-jose[cryptography]==3.3.0

# Date/Time utilities
python-dateutil==2.8.2

# Email (for notifications)
email-validator==2.1.0
